{
  colorschemes = {
    # catppuccin.enable = true; # good
    # gruvbox.enable = true; # too muted
    # tokyonight.enable = true; # too many similar colors
    onedark = {
      # good
      enable = true;
      settings = {
        colors = {yellow = "#d9a950";};
        style = "dark";
        transparent = true;
      };
    };
  };
}
